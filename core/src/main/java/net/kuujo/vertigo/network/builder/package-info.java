/**
 * Contains builder classes for building network configurations.
 * The builder classes are useful for defining a network configuration in code (as opposed to json configuration).
 */
package net.kuujo.vertigo.network.builder;